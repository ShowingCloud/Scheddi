<%= render 'refinery/admin/search_header', :url => refinery.scheddi_recommendations_admin_scheddi_recommendations_path %>
<div class='pagination_container'>
  <% if @scheddi_recommendations.any? %>
    <%= render 'scheddi_recommendations' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
